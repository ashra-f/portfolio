// Shared mutable state between stars.js (renders the starfield) and
// scrollCinema.js (spikes the speed on scroll for a warp-speed effect).
export const starsParams = { speed: 1.5, number: 300, extinction: 4 };
